import React from "react";

import { render, fireEvent } from "@testing-library/react";

import MainView from "../Views/Main";

describe("<MainView />", () => {
	test("Should show the filters and everything that it includes.", async () => {});
});
